export declare type ListOptions = 'basic' | 'standard' | 'extended';
export interface Gender {
    label: string;
    value: string;
    list: ListOptions;
}
export interface GenderOption {
    label: string;
    value: string;
}
export declare const genders: Gender[];
