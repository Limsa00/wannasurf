"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const genders_1 = require("./lib/genders");
const sort_1 = require("./lib/sort");
function GenderOptions(genders, listName) {
    const list = genders
        .filter((gender) => gender.list === listName)
        .map(({ label, value }) => ({ label, value }));
    sort_1.sortGenders(list);
    return list;
}
const basic = GenderOptions(genders_1.genders, 'basic');
const standard = basic.concat(GenderOptions(genders_1.genders, 'standard'));
const extended = standard.concat(GenderOptions(genders_1.genders, 'extended'));
exports.genderOptions = {
    basic,
    standard,
    extended,
};
//# sourceMappingURL=index.js.map